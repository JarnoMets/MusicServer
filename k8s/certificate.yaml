apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: music-cert
  namespace: music
spec:
  secretName: music-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
  dnsNames:
    - music.jarnomets.com


