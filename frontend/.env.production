# Production API URL
# When running in Docker, the frontend proxies through nginx
# which forwards to http://backend:8081
# So the frontend client should use relative paths or the container hostname
VITE_API_URL=/api
